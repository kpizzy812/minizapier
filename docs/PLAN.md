# План: Платформа автоматизации (мини-Zapier)

## Решения по архитектуре

| Вопрос | Решение |
|--------|---------|
| Авторизация | Supabase Auth Cloud (free plan, JWT, email/password) |
| Вау-фишки | Условные ветвления + Визуальный дебаггер |
| Email триггер | Webhook (SendGrid/Mailgun inbound) |
| Деплой | VPS + Docker Compose + домен |
| БД/Redis | Свои в Docker (не облако) |

---

## Анализ ТЗ

### Функциональные требования
- Визуальный редактор workflow (drag-and-drop)
- 3 типа триггеров: Webhook, Расписание (cron), Email
- 5 типов действий: HTTP запрос, Email, Telegram, БД, Трансформация данных
- Логирование шагов выполнения
- Обработка ошибок: retry, уведомления, пауза
- Дашборд: список workflows, история, статистика

### Технические требования
- Очередь задач (BullMQ)
- Изоляция выполнения workflows
- REST API + Swagger документация

---

## Технологический стек

### Backend
- **NestJS** - модульный фреймворк, Swagger, интеграция с BullMQ
- **BullMQ** - очередь задач с retry, delayed jobs, изоляция процессов
- **PostgreSQL** - своя БД в Docker
- **Redis** - для BullMQ (свой в Docker)
- **Prisma** - ORM
- **Supabase Auth** - только аутентификация (JWT, email/password)

### Frontend
- **Next.js 16.1** - React 19, Turbopack (stable), `use cache`, PPR
- **React Flow** - визуальный редактор workflow
- **TailwindCSS 4 + shadcn/ui** - UI компоненты
- **TanStack Query v5** - серверное состояние
- **Zustand** - клиентское состояние
- **@supabase/ssr** - auth на сервере и клиенте

### Инфраструктура
- **Docker Compose** - PostgreSQL, Redis, API, Web (всё локально)
- **VPS деплой** - Docker Compose + Traefik/Caddy (SSL, домен)
- **WebSocket (Socket.io)** - real-time статус выполнения

---

## Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (Next.js)                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  Дашборд    │  │  Редактор   │  │  История/Логи       │  │
│  │  workflows  │  │  React Flow │  │  выполнения         │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          │ REST API / WebSocket
┌─────────────────────────▼───────────────────────────────────┐
│                     Backend (NestJS)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ Workflows    │  │ Triggers     │  │ Executions       │   │
│  │ Module       │  │ Module       │  │ Module           │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ Actions      │  │ Queue        │  │ Notifications    │   │
│  │ Module       │  │ Module       │  │ Module           │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                    Data Layer                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ PostgreSQL   │  │ Redis        │  │ BullMQ Workers   │   │
│  │ (Prisma)     │  │ (Queue)      │  │ (Isolated)       │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## Модули Backend

### 1. Workflows Module
- CRUD операции для workflows
- Валидация схемы workflow
- Версионирование (история изменений)

### 2. Triggers Module
- **WebhookTrigger** - генерация уникальных URL, прием данных
- **ScheduleTrigger** - cron выражения, планировщик
- **EmailTrigger** - парсинг входящих писем (IMAP/webhook)

### 3. Actions Module (Plugin-архитектура)
- **HttpAction** - HTTP запросы с настройкой headers, body, auth
- **EmailAction** - отправка через SMTP/API (SendGrid, Resend)
- **TelegramAction** - Bot API интеграция
- **DatabaseAction** - SQL запросы к внешним БД
- **TransformAction** - JSONPath, JavaScript выражения

### 4. Execution Engine
- Orchestrator выполнения шагов
- Контекст выполнения (передача данных между шагами)
- Обработка ошибок и retry логика
- Изоляция в worker процессах

### 5. Logging & Monitoring
- Детальные логи каждого шага
- Метрики выполнения
- Real-time статус через WebSocket

---

## "Вау" фишки (приоритетные)

### 1. Условные ветвления (If/Else) ⭐ ПРИОРИТЕТ
```
           ┌──[condition: true]──→ [Action A] ──┐
[Trigger]──┤                                    ├──→ [Final]
           └──[condition: false]─→ [Action B] ──┘
```
- Специальная нода `ConditionNode` с несколькими выходами
- Поддержка выражений: `{{data.status}} === 200`, `{{data.amount}} > 100`
- JavaScript-подобный синтаксис в песочнице (vm2/isolated-vm)
- Параллельное выполнение веток (Promise.all)

### 2. Визуальный дебаггер (Real-time) ⭐ ПРИОРИТЕТ
```
┌─────────────────────────────────────────┐
│  [Webhook] ✅ → [Transform] 🔄 → [HTTP] ⏳  │
│     ↓              ↓                     │
│   input:        output:                  │
│   {body:...}    {mapped:...}            │
└─────────────────────────────────────────┘
```
- WebSocket подключение к execution engine
- Подсветка текущей ноды (pending/running/success/error)
- Панель с input/output данными каждого шага
- Кнопка "Replay" - перезапуск с любого шага
- Timeline выполнения с длительностью

### 3. Дополнительные фишки (бонус)
- **Webhook тестер** - отправка тестовых запросов из UI
- **Переменные** - глобальные переменные workflow `{{vars.apiKey}}`
- **Retry UI** - визуальная настройка retry стратегии на ноде

---

## Критические фичи (без них проект фуфло)

| Фича | Почему критична |
|------|-----------------|
| **Credentials хранилище** | Безопасное хранение API-ключей, токенов, паролей (AES-256) |
| **Визуальный маппер данных** | UX как в Zapier: клик → дерево данных → выбор поля |
| **Шаблоны переменных** `{{trigger.body.name}}` | Без передачи данных между шагами workflow бесполезен |
| **Валидация перед сохранением** | Нельзя сохранить сломанный workflow (нет триггера, разрыв цепочки) |
| **Тестовый запуск** | Пользователь должен проверить workflow до активации |
| **Retry с backoff** | HTTP запросы падают, без retry workflow ненадёжен |
| **Логи с фильтрацией** | Быстрый поиск проблем в истории выполнений |
| **Активация/деактивация** | Возможность "выключить" workflow без удаления |
| **Копирование workflow** | Создание на основе существующего |
| **Автосохранение** | Потеря работы = злой пользователь |

---

## Credentials (хранилище секретов)

```
┌─────────────────────────────────────────────────────┐
│  Мои подключения                                    │
├─────────────────────────────────────────────────────┤
│  🤖 Telegram Bot "Notifications"     [Изменить]     │
│  📧 SMTP "Company Email"             [Изменить]     │
│  🔗 HTTP Auth "External API"         [Изменить]     │
│  🗄️ PostgreSQL "Analytics DB"        [Изменить]     │
│                                                     │
│  [+ Добавить подключение]                           │
└─────────────────────────────────────────────────────┘
```

### Типы подключений
| Тип | Поля | Использование |
|-----|------|---------------|
| `telegram` | bot_token | Telegram Action |
| `smtp` | host, port, user, password | Email Action |
| `http_basic` | username, password | HTTP Action (Basic Auth) |
| `http_bearer` | token | HTTP Action (Bearer) |
| `http_api_key` | key, header_name | HTTP Action (API Key) |
| `database` | connection_string | Database Action |
| `resend` | api_key | Email Action (Resend) |

### Шифрование
- **AES-256-GCM** для значений
- Ключ из env `ENCRYPTION_KEY` (32 bytes)
- Уникальный IV для каждого credential

---

## Визуальный маппер данных (Data Picker)

### UX Flow
```
┌─────────────────────────────────────────────────────┐
│  HTTP Request                                       │
├─────────────────────────────────────────────────────┤
│  URL: [https://api.example.com/users/[•]          ]│
│                                      ↓              │
│       ┌─────────────────────────────────────┐       │
│       │ 📦 Доступные данные                 │       │
│       ├─────────────────────────────────────┤       │
│       │ ▼ webhook (trigger)                 │       │
│       │   ├─ headers                        │       │
│       │   │  └─ content-type                │       │
│       │   └─ body                           │       │
│       │      ├─ user                        │       │
│       │      │  ├─ id        ← [Вставить]   │       │
│       │      │  ├─ email                    │       │
│       │      │  └─ name                     │       │
│       │      └─ action                      │       │
│       │                                     │       │
│       │ ▼ transform_1 (шаг 2)              │       │
│       │   └─ result                         │       │
│       └─────────────────────────────────────┘       │
│                                                     │
│  Body: {"userId": "{{webhook.body.user.id}}"}      │
└─────────────────────────────────────────────────────┘
```

### Как это работает
1. **Тестовый запуск** → сохраняем sample data каждого шага в workflow
2. **Клик на input** → открывается Data Picker с деревом данных
3. **Дерево показывает** реальную структуру из sample data предыдущих шагов
4. **Выбор поля** → вставляется `{{stepId.path.to.field}}`
5. **Preview** → показывает реальное значение из sample data

### Компонент DataPicker
```typescript
interface DataPickerProps {
  // Sample data из предыдущих шагов
  availableData: Record<string, unknown>;
  // Callback при выборе пути
  onSelect: (path: string) => void;
  // Текущая позиция курсора в input
  cursorPosition: number;
}
```

### Sample Data Storage
```prisma
model Workflow {
  // ... existing fields
  sampleData  Json?    // Сохранённые данные последнего тестового запуска
}
```

---

## Структура проекта

```
minizapier/
├── apps/
│   ├── api/                      # NestJS Backend
│   │   ├── src/
│   │   │   ├── modules/
│   │   │   │   ├── auth/         # Supabase JWT validation
│   │   │   │   ├── workflows/    # CRUD, версионирование
│   │   │   │   ├── credentials/  # Хранилище секретов (AES-256)
│   │   │   │   ├── triggers/     # Webhook, Cron, Email
│   │   │   │   ├── actions/      # HTTP, Email, Telegram, DB, Transform
│   │   │   │   ├── executions/   # Engine, логи, статистика
│   │   │   │   ├── queue/        # BullMQ workers
│   │   │   │   └── gateway/      # WebSocket для real-time
│   │   │   ├── common/           # Guards, decorators, pipes
│   │   │   └── main.ts
│   │   └── prisma/
│   │       └── schema.prisma
│   │
│   └── web/                      # Next.js Frontend
│       ├── src/
│       │   ├── app/
│       │   │   ├── (auth)/       # Login, signup (Supabase)
│       │   │   ├── (dashboard)/  # Список workflows, статистика
│       │   │   └── editor/[id]/  # React Flow редактор
│       │   ├── components/
│       │   │   ├── editor/       # Кастомные ноды React Flow
│       │   │   ├── data-picker/  # Визуальный маппер данных
│       │   │   ├── debugger/     # Панель дебаггера
│       │   │   ├── credentials/  # Управление подключениями
│       │   │   └── ui/           # shadcn/ui
│       │   ├── lib/
│       │   │   ├── supabase/     # Client/server клиенты
│       │   │   └── api.ts        # API клиент
│       │   └── stores/           # Zustand stores
│       └── ...
│
├── packages/
│   └── shared/                   # Типы нод, workflow schema
│       ├── src/
│       │   ├── types/
│       │   └── validators/
│       └── package.json
│
├── docker-compose.yml            # PostgreSQL, Redis
├── turbo.json
└── package.json                  # pnpm workspaces
```

---

## База данных (Prisma Schema)

```prisma
// Связь с Supabase Auth
model Workflow {
  id          String   @id @default(cuid())
  userId      String   // Supabase auth.users.id
  name        String
  description String?
  definition  Json     // {nodes: [...], edges: [...], variables: {...}}
  sampleData  Json?    // Данные последнего тестового запуска для Data Picker
  isActive    Boolean  @default(false)
  version     Int      @default(1)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  trigger     Trigger?
  executions  Execution[]
}

// Хранилище секретов (credentials)
enum CredentialType {
  TELEGRAM
  SMTP
  HTTP_BASIC
  HTTP_BEARER
  HTTP_API_KEY
  DATABASE
  RESEND
}

model Credential {
  id          String         @id @default(cuid())
  userId      String         // Supabase auth.users.id
  name        String         // "My Telegram Bot"
  type        CredentialType
  data        String         // AES-256-GCM encrypted JSON
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt

  @@index([userId])
}

enum TriggerType {
  WEBHOOK
  SCHEDULE
  EMAIL
}

model Trigger {
  id          String      @id @default(cuid())
  type        TriggerType
  config      Json        // webhook: {secret}, schedule: {cron}, email: {address}
  webhookUrl  String?     @unique // Уникальный URL для webhook
  workflowId  String      @unique
  workflow    Workflow    @relation(fields: [workflowId], references: [id], onDelete: Cascade)
}

enum ExecutionStatus {
  PENDING
  RUNNING
  SUCCESS
  FAILED
  PAUSED
}

model Execution {
  id         String          @id @default(cuid())
  workflowId String
  workflow   Workflow        @relation(fields: [workflowId], references: [id], onDelete: Cascade)
  status     ExecutionStatus @default(PENDING)
  startedAt  DateTime        @default(now())
  finishedAt DateTime?
  input      Json?           // Входные данные триггера
  output     Json?           // Финальный результат
  error      String?         // Ошибка если FAILED

  steps      StepLog[]

  @@index([workflowId, startedAt])
}

model StepLog {
  id          String    @id @default(cuid())
  executionId String
  execution   Execution @relation(fields: [executionId], references: [id], onDelete: Cascade)
  nodeId      String    // ID ноды из React Flow
  nodeName    String    // Название для отображения
  status      String    // pending, running, success, error, skipped
  input       Json?
  output      Json?
  error       String?
  duration    Int?      // Длительность в ms
  createdAt   DateTime  @default(now())

  @@index([executionId, createdAt])
}
```

---

## Roadmap: 2 дня интенсивной работы

### День 1: Фундамент + Редактор + Engine

**Блок 1: Инфраструктура (~2ч)**
- [ ] Monorepo (pnpm workspaces + turbo)
- [ ] Docker Compose (PostgreSQL, Redis)
- [ ] NestJS scaffold + Swagger + Prisma
- [ ] Next.js 16 + TailwindCSS 4 + shadcn/ui
- [ ] Supabase Auth интеграция

**Блок 2: Визуальный редактор (~4ч)**
- [ ] React Flow настройка
- [ ] Кастомные ноды: TriggerNode, ActionNode, ConditionNode
- [ ] Sidebar с палитрой (drag-and-drop)
- [ ] **Data Picker** - визуальный маппер данных (дерево, выбор поля)
- [ ] Сохранение/загрузка workflow API
- [ ] Валидация схемы

**Блок 3: Execution Engine (~3ч)**
- [ ] BullMQ интеграция
- [ ] Workflow executor (обход графа, топологическая сортировка)
- [ ] Контекст выполнения (данные между нодами)
- [ ] Условные ветвления (if/else)
- [ ] Логирование шагов в БД

---

### День 2: Триггеры + Actions + Дебаггер + Деплой

**Блок 4: Триггеры (~2ч)**
- [ ] Webhook триггер (уникальный URL)
- [ ] Schedule триггер (cron через BullMQ repeatable)
- [ ] Email триггер (webhook от провайдера)

**Блок 5: Credentials + Actions (~3ч)**
- [ ] **Credentials модуль** (CRUD, AES-256 шифрование)
- [ ] **Credentials UI** (список, создание, редактирование)
- [ ] HTTP Request Action (с выбором credential)
- [ ] Email Action (Resend API)
- [ ] Telegram Action (Bot API)
- [ ] Database Action (SQL запросы)
- [ ] Transform Action (JSONPath, JS expressions)

**Блок 6: Real-time дебаггер (~2ч)**
- [ ] WebSocket Gateway
- [ ] Подсветка выполняемых нод
- [ ] Панель input/output
- [ ] Replay с любого шага

**Блок 7: Дашборд + Деплой (~1.5ч)**
- [ ] Список workflows + история
- [ ] Статистика выполнений
- [ ] Docker production build
- [ ] Деплой на VPS (Traefik + SSL)

---

## Типы нод (React Flow)

| Тип | Категория | Описание |
|-----|-----------|----------|
| `WebhookTrigger` | Trigger | Входящий HTTP запрос |
| `ScheduleTrigger` | Trigger | Cron расписание |
| `EmailTrigger` | Trigger | Входящее письмо |
| `HttpRequest` | Action | Исходящий HTTP запрос |
| `SendEmail` | Action | Отправка email |
| `SendTelegram` | Action | Сообщение в Telegram |
| `DatabaseQuery` | Action | SQL запрос |
| `Transform` | Action | Трансформация данных |
| `Condition` | Logic | If/Else ветвление |

---

## Верификация

```bash
# 1. Запуск инфраструктуры
docker-compose up -d

# 2. Запуск dev серверов
pnpm dev

# 3. Создание workflow через UI
# - Открыть http://localhost:3000
# - Залогиниться через Supabase Auth
# - Создать workflow с Webhook → Transform → HTTP

# 4. Тест webhook
curl -X POST http://localhost:3001/webhooks/{id} \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'

# 5. Проверить
# - Логи выполнения в дашборде
# - Real-time дебаггер в редакторе
# - Swagger: http://localhost:3001/api/docs
```

---

## API Endpoints (основные)

```
POST   /auth/login              # Supabase JWT
POST   /auth/refresh

GET    /workflows               # Список workflows пользователя
POST   /workflows               # Создать workflow
GET    /workflows/:id           # Получить workflow
PUT    /workflows/:id           # Обновить
DELETE /workflows/:id           # Удалить
POST   /workflows/:id/activate  # Активировать
POST   /workflows/:id/test      # Тестовый запуск (сохраняет sampleData)
POST   /workflows/:id/duplicate # Копировать workflow

GET    /credentials             # Список подключений
POST   /credentials             # Создать подключение
GET    /credentials/:id         # Получить (без secrets)
PUT    /credentials/:id         # Обновить
DELETE /credentials/:id         # Удалить
POST   /credentials/:id/test    # Проверить подключение

GET    /executions              # История выполнений
GET    /executions/:id          # Детали выполнения
GET    /executions/:id/logs     # Логи шагов
POST   /executions/:id/replay   # Перезапуск

POST   /webhooks/:token         # Публичный webhook endpoint
```
